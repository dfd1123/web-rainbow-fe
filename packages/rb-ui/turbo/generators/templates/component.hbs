import React from "react";
import { makeCxFunc } from "@packages/rb-utils/parserUtils";

import style from "./{{pascalCase name}}.module.scss";

type Props = {
  className?: string;
}

const cx = makeCxFunc(style);

const {{ pascalCase name }} = ({ className = '' }: Props) => {
  return (
    <div className={cx('{{kebabCase name}}', className)}>
      {{ pascalCase name }}
    </div>
  );
};

export default {{ pascalCase name }};